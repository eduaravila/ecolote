import {StyleSheet} from 'react-native';
import {normalize} from '../../../../style/UTILS';

export const styles = StyleSheet.create({
  google_icon: {
    fontWeight: 'bold',
  },
  button: {marginBottom: normalize(15)},
  title: {marginBottom: normalize(50)},
});
